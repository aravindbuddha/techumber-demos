import template from './volume-control.html';
import controller from './volume-control.controller';
import './volume-control.css';

let volumeControlComponent = {
		template,
		controller
};

export default volumeControlComponent;
